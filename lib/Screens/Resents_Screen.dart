import 'package:flutter/material.dart';

class Resents extends StatelessWidget {
  const Resents({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Calls", style: TextStyle(color: Colors.white)),
    );
  }
}
